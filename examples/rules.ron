Table(
  chains: {
    "start": Chain(
      rules: [
        Rule(
          predicate: True,
          action: SetSrcAccount("assets::account::unknown"),
        ),
        Rule(
          predicate: True,
          action: SetSrcAccount("assets::account::unknown"),
        ),
      ],
    ),
  },
)
