Table(
  chains: {
    "start": Chain(
      rules: [
        Rule(
          predicate: True,
          action: SetSrcAccount("assets:unknown"),
          result: Continue,
        ),
        Rule(
          predicate: True,
          action: SetDestAccount("expenses:unknown"),
          result: Continue,
        ),
      ],
    ),
  },
)
